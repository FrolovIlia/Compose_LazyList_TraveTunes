<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/color_background"
    android:minHeight="96dp">

    <ProgressBar
        android:id="@+id/progressBar"
        style="@android:style/Widget.DeviceDefault.Light.ProgressBar.Large"
        android:layout_width="48dp"
        android:indeterminate="true"
        app:layout_constraintBottom_toBottomOf="@id/progress_text"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@id/progress_text"
        android:layout_margin="@dimen/spacing_24"
        android:layout_height="48dp"
        android:backgroundTint="@color/main_black"
        android:foregroundTint="@color/main_white"
        android:indeterminateTint="@color/main_blue"
        android:progressBackgroundTint="@color/main_black"
        android:progressTint="@color/main_black"
        android:secondaryProgressTint="@color/main_black"
        />

    <androidx.appcompat.widget.AppCompatImageView
        android:id="@+id/logoImg"
        android:layout_width="25dp"
        android:layout_height="25dp"
        app:layout_constraintBottom_toBottomOf="@id/progressBar"
        app:layout_constraintEnd_toEndOf="@id/progressBar"
        app:layout_constraintStart_toStartOf="@id/progressBar"
        app:layout_constraintTop_toTopOf="@id/progressBar"
        app:srcCompat="@drawable/pic_splash_logo" />

    <TextView
        android:id="@+id/progress_text"
        style="@style/FontRegular.Gray18"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/spacing_24"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@id/progressBar"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="Please waiting" />
</androidx.constraintlayout.widget.ConstraintLayout>